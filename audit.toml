[advisories]
ignore = [
    # async-std is discontinued (RUSTSEC-2025-0052) — informational notice, not a CVE.
    # This is a transitive dependency; track removal separately.
    "RUSTSEC-2025-0052",
]
