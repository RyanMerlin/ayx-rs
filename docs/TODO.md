# TODO

This file is retained for compatibility.

The active roadmap now lives under `docs/roadmap/`.
Use that tree for current work items and `docs/adr/` for durable decisions.
Keep temporary handoffs in `docs/handoffs/` or local temp storage.
