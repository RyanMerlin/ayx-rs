# Draft: ayx v0.17.0-internal.1 rollout announcement

> Draft for review. Do not send until the v0.17.0-internal.1 validation is green
> and the release owner approves the audience and timing.

## ayx v0.17.0-internal.1 is ready for internal evaluation

We are preparing `ayx` v0.17.0-internal.1 for internal rollout. This release
makes the Wizard authentication orchestration the default for `ayx one login`
while keeping the legacy email-OTP adapter available as an explicit rollback
path.

The Alteryx One surface has also been re-verified across authentication,
identity, workspaces, plans, scheduling, connections, datasets, jobs,
workflows, and reversible CRUD probes. Remaining OpenAPI coverage gaps are
documented roadmap items rather than hidden failures.

### What to try

- Install the internal Windows or WSL2/Linux artifact supplied by the release
  owner. Apple/macOS is deferred for this rollout.
- Configure the regional host for the workspace, for example
  `https://<region>.alteryxcloud.com`; the region is workspace-specific.
- Run `ayx onboard`, paste the workspace URL, and choose **Log in now**.
- Run `ayx one login` with secure keyring persistence when available.
- Use `--secret-policy session` for a non-persistent evaluation, or choose the
  explicit plaintext fallback only when the environment cannot use a secure
  store.

### Rollback

If authentication behaves unexpectedly, retry explicitly with
`ayx one login --auth-flow legacy` (or set `AYX_AUTH_ROLLOUT=legacy` for
automation). Please include the command, platform, region, redacted error
envelope, and rollout setting in feedback. Do not share OTPs, passwords,
access tokens, profile files, or keyring exports.

Live validation uses the existing test profile and account. The default
Wizard lane is the ordinary internal release path, with Legacy retained as the
explicit rollback lane.

### Platform note

Native Windows and WSL2/Linux are the required internal sign-off platforms.
Apple/macOS validation and distribution are deferred.

Please report issues through the repository issue tracker or the internal
ayx support channel, including the redacted diagnostics described above.
