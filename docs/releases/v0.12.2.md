# AYX-RS v0.12.2 Release Notes

## Fix

**`ayx update` now finds the Linux and macOS binary in its release archive.** Tar packaging previously stored the member as `./ayx`, while `self_update` compared it exactly with `ayx`; members are now packaged explicitly at the archive root. Windows `.zip` behavior was already corrected in v0.12.1.

